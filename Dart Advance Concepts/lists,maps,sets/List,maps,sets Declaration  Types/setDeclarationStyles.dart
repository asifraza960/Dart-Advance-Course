void main(List<String> args) {
  //simply declaring a set as its syntax to declare a set
  // we can declare sets with multiple styles as we declare for lists

  Set<int> mySet = <int>{1, 2, 3, 4, 5};
  print(mySet);
  print(mySet.runtimeType);
}
